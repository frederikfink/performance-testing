query Article($slug: String) {
  article(filter: { slug: { eq: $slug } }) {
    __typename
    id
    slug
    title
    subtitle
    heroImage {
      focalPoint {
        x
        y
      }
      responsiveImage(
        imgixParams: { crop: focalpoint, w: "3600", h: "200", fit: crop }
      ) {
        ...Image
      }
    }
    content {
      value
      blocks {
        ...BlogMedia
        ...BlogCallout
        ...BlogCodeBlock
      }
    }
  }
}
